const initialState = {
  menuButtons: {
    profile: "профиль",
    skills: "навыки",
    works: "мои работы",
    dialogs: "общение",
  },
};

export const uiReducer = (state = initialState, action) => {
  return state;
};
